# Let Railway auto-detect Node.js version from package.json
# Using simpler config for better compatibility

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
